# 작업 이력 — v4 모델별 전처리 + 피드백 반영 (2026-06-20~21)

[01_전체_작업_히스토리](01_전체_작업_히스토리.md) 이후의 작업을 빠짐없이 기록. 사용자 피드백/질문과 그에 대한 처리·결론 포함.

---

## A. 피드백 6건 반영 (→ [reports/20])
1. **백엔드 최소화**: 33파일 다층 트리 → `backend/` 평면 8파일(server.js/db.js/check.js/schema_mysql.sql/seed.sql/package.json/.env.example/CONTRACT.md). 무설치 실행·검증 통과.
2. **모델→서버 공유변수 계약**: `backend/CONTRACT.md` 작성(`/models/submit` 정확한 JSON).
3. **과거 전처리 버전폴더 보강 + 데이터셋 백업**: legacy/aux 8폴더 신설.
4. **models7 재진행(하나씩)** + 우려 검증: v4 신설.
5. **리포트19 수정**: 웹=Streamlit(둘 다)·서버선택. 05-6 구조 평면화.
6. 진행결과 리포트(20).

## B. 데이터셋 물리 백업 (유실 복원)
- `sample_project/data/processed_5m`(96M, models7 포함) → `preprocessing_project/v2_5m/output/processed_5m/` 복사.
- 과거 작업 8종 + v1_daily 일별까지 전부 백업, **총 386M**. 스크립트 `preprocessing_project/backup_datasets.py`(`--all`), 마스터 `DATASET_INDEX.md`.

## C. v4_model_prep — 모델 하나씩(모델별 폴더)
- 소스: `models_bayes_one.py`(전처리 scaler/log/imbalance + HP 베이지안), `realtime_compat_test.py`, `transformer_seq_prep.py`, `demo_session_preview.py`, `inspect_one_user.py`(v2_5m/src).
- 입력: `train/test_cohort_tabular.parquet`(코호트 recency≤7, **10피처 realtime-safe**).
- 결과(Feb 시간외삽 AUC): XGBoost **0.7904**(최고) / LightGBM·CatBoost 0.7902 / RandomForest 0.7892 / LogReg 0.7860 / DecisionTree 0.7773 / Transformer val 0.7855(norm=log).
- 각 모델 폴더(`output/<Model>/`): `prep_*.joblib`(전처리+모델+isotonic보정+임계값) · `*_bayes.json` · `*_{train,test}.parquet`(전처리본 백업) · `*_전처리리포트.md`.
- 전 모델 **실시간/10컬럼 호환 테스트 PASS**.

## D. 사용자 질문/지적과 결론 (중요)
| 질문 | 결론 |
| --- | --- |
| cohort_tabular이 7일로 압축된 것? | ❌ 아님. recency≤7는 **유저 선택**, 피처는 **관찰기간 전체 집계**. 실유저(396059467) tenure 114일·98활동일로 증명([inspect_one_user]). |
| v4 parquet은 코호트 기준? | ✅ 맞음(109,378행, 전체 1.27M의 8.6%). |
| v4가 바운스/초단위/추천 되나? | ❌ 불가. v4 입력에 시각·상품·세션 신호 없음 + 코호트 91% 제외([reports/21]). |
| v4 src에 전처리 소스? | `models_bayes_one.py`는 **집계 10피처 재변환만**. raw→피처는 `v2_5m/src/prep_5month.py`. |
| 17-7-1 만족? | 🔶 부분. 전처리 기반은 채우나 임베딩·**DL→GBM 스태킹**·Brier/ECE·MTL 미구현. |
| 시간별 트랜잭션 증발? | tabular는 평균(증발 맞음). **event/세션 뷰는 시각·상품·Δt 보존**(데모/원본CSV 증거). |
| 5m 호환? | ✅ 추가형. 단 ①시간경계 재사용 ②코호트 정렬 플래그 + 1원천 2뷰. |

## E. 산출 데모/증거 파일
- `preprocessing_project/v2_5m/output/sample_user_inspect/` — 활성 유저 원본 1,054행 + 집계행 + 주별시퀀스(엑셀).
- `preprocessing_project/v4_model_prep/output/_preview/session_event_preview.{png,csv}` — 이벤트/세션 시간보존 미리보기(바운스·churn30·Δt·cat/brand).

## F. 신규/갱신 리포트
- 20(피드백반영), 21(v4한계·이벤트세션 미리보기), **22(전처리 화이트페이퍼)**, **23(v4 개선안 계획서)**.

## G. 다음 (계획서 23의 범위 선택 대기)
- (A) build_eventbase 시간보존·2뷰 → (B) 7모델 v2 재실행 → (C) 세션 스코어러·추천 시연 → (D) 17-7-1 스태킹. 권고 A→C.
