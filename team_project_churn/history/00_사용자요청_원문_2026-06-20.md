# 사용자 요청 원문 (2026-06-20)

> 아래는 사용자가 보낸 요청 텍스트 그대로의 보존본이다.

---

1- C:\Users\playdata2\Documents\ml_workspace\team_project_churn
위 폴더의 스크립트 파일들 전부 스크립트폴더 만들어서 이동시켜

2- 셈플프로젝트 폴더 따로 있는 거 처럼 전처리 프로젝트 ㄴ폴더를 따로 만들어서 전처리기 관련된 파일들과 폴더를 전처리기 폴더에다가 넣어.
- 그리고 전처리프로젝트 폴더에는 전처리기 버전별로 내부 폴더가 나와줘야 해.
- 이 폴더들에서 각각의 전처리기 결과물들이 있어야 하고
- 전처리 결과물 파일들을 언제든지 재현해서 다시 만들 수 있게 하는 소스파일들이 다 해당 폴더들에 저장되어 있어야 하고
- 어떤식으로 해당 폴더의 전처리기가 구성되어 있고 어떤 컬럼들이 있고 x y 구성을 어떻게 했는지 등등의 자세한 사항을 한눈에 볼 수 있는 md 파일이 있어야 해.

3- 전처리를 위한 정리가 필요해. 우선 기존의 C:\Users\playdata2\Documents\ml_workspace\team_project_churn\src\2019-Dec.csv.zip
위 REES46 화장품 데이터 2019 10 에서 2020 2 까지의 데이터셋의 특징을 정리하고 분석까지 완벽하게 마쳐야 해.
발표할 때 무리 없게 정확하게 자세하고 전문가 입장에서도 납득가게 작성이 되어야 하고 해당 자료는 리포트파일로 제출해.
- 전처리 작업을 어떻게 해야 5m 과 호완성이 유지되면서 모델7 작업에서 각각의 모델에서 최고의 성능을 낼 수 있을지 REES46 최신 이탈예측 논문들을 리서치하고 찾아서 베이지안 서치를 적용해서  결과물을 뽑아줘야 해. 이 부분에 대해서 계획서 파일 작성해서 제출해.
- 이 때 7가지의 모델과 추가로 더 발전된 형태의 최신 모델들의 가장 최고의 최적화 작업을 위한 작업도 진행해야 해서 모든 모델 통틀어서 최고의 효율과 퍼포먼스를 낼 수 있는 구조는 어떤건지도 리서치가 진행되어야 해.
C:\Users\playdata2\Documents\ml_workspace\team_project_churn\search.txt
위 파일에서 처음 ---- 실선 전까지의 내용을 분석하고 너가 리서치한 것과 비교해서 최선의 방법 리포트해봐
- 위 작업 2개와 별개로 전처리 프로젝트 폴더에서 우리가 이전에 작업한 모델7 작업물들을 5m 과 호완되고
정리: 신규 디렉토리·신규 파일·스키마 버전·DB 판별컬럼(기본값)·신규 model_type 으로 얹기만 하고, 기존 일별 배포본은 손대지 않는 게 호환 안전한 방식입니다.

원하시면 이 원칙대로 ① seq_type 컬럼 추가 마이그레이션 + ② build_event_sequence.py(전처리만) 를 기존 것 안 건드리고 추가하고, 회귀 스모크까지 돌려 기존 배포본이 멀쩡한지 확인해 드리겠습니다. 진행할까요?
너가 말햇던 위 사항이 적용된 버전을 먼저 뽑고 테스트 진행해봐.

4- 이제 백엔드 관련 작업도 들어가야 해. 이전에 나온 5m 의 구조와 데이터들을 가지고 백엔드-db 프로그램도 나와야 해. 지금 변경 된 구조에 맞게
C:\Users\playdata2\Documents\ml_workspace\team_project_churn\reports\05-5_프로그램_구조도_및_함수_변수_비전_명세서.md
위 파일 참고해서 다음 버전 05-6 을 작성하고 그걸 기반으로 백엔드-db(mysql 서버 사용) 이 나와줘야해. 해당 부분은 새로운 리포트로 제출해야 하니까 2개의 리포트가 추가 되어야 해.
- 백엔드 서버 구조는 각 모델파트 진행자들이 사전에 전처리된 파일들을 가지고 학습된 모델이 나오면 그 결과값 변수들을 백엔드 서버에 제출하고 이 값들을 이용해서 스트림릿 프론트엔드 앱에 필요한 자료들을 제공하고 스트림릿의 화면에 결과들을 보여주는 방식으로 진행할 거야.
- 그리고 simulation site 에서 유저 활동 시뮬레이션을 진행하고 로그를 neon 서버에 저장하고 이 로그를 우리 백엔드 서버에서 가져와서 학습된 모델들을 통해서 선택된 모델들에 대한 실시간으로 연산되는 이탈율과 원인분석을 해서 표시해주고 앙상블을 진행해서 어떤점들을 개선해야하는지 표시해 주는 방식으로 진행할 거야.
- 그리고 여유가 되면 이걸 실제로 다시 neon 서버로 보내서 쿠폰 제공이나 관련 상품 추천과 같은 걸 동해 이탈예측->이탈방지 가 되게 하는 것까지 시연하면 베스트라고 생각해.
- 이 4번 문항의 작업들도 별도의 스레드를 하나 할당해서 진행하도록 해.
- 백엔드 서버는 노드js 생각중인데 추천 서버 있으면 알려줘
- 생성된 리포트를 바탕으로 위 작업을 어떻게 진행할 건지 계획서 작성하고 제출한 뒤에 해당 계획서를 기반으로 작업을 진행하고 결과물이 나오면 계획서에 맞게 작업이 진행되었는지 체크해서 완벽해 질때까지 다시 작업해야 해.

5- 히스토리 폴더 만들어서 지금 내가 요청한 사항들은 텍스트 그대로 남기고 지금까지 작업한 모든 사항들 다 정리해서 하나도 빠짐없이 히스토리 파일들로 남겨줘
