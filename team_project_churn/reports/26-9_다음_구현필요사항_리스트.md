# 26-9. 다음 구현 필요사항 리스트 (잔여 작업)

기준: 2026-06-21. 현재까지 완료(대시보드·얼굴로그인·교육과제5·바운스·백엔드 MySQL 실연동·피처중요도)를 제외한 **남은 구현 항목**을 우선순위로 정리.

---

## P1 — 발표 직결 (권장 즉시)
| # | 항목 | 내용 | 위치 |
| --- | --- | --- | --- |
| 1 | **대시보드 → 백엔드 REST 전면 연결** | 현재 대시보드가 eval 파일을 직접 읽음 → `api_client.py`로 backend `/dashboard/summary`·`/models/:m/charts/:name` 호출로 교체(19-1 §9.3, 완료기준) | 가지마 `app/services/api_client.py` |
| 2 | **백엔드 서버 상시 기동 + 대시보드 연동 검증** | `node src/server.js`(MySQL 모드) 띄우고 대시보드가 REST로 차트 받기 | backend + dashboard |
| 3 | **모델 7종 일괄 제출** | `pp_eval_package` 결과를 `POST /models/submit`로 7모델 registry·evaluation 적재(스크립트화) | `src/submit_models.py` |
| 4 | **고위험 top-risk / latest 예측 API** | `/predictions/top-risk`·`/predictions/latest` 구현 + 대시보드 패널 | backend |

## P2 — 실시간 시연(이탈예측→방지 루프)
| # | 항목 | 내용 |
| --- | --- | --- |
| 5 | **simulation_site** | Vercel/정적 이커머스 시뮬 + `/events` → Neon `sim_event_log` 적재 |
| 6 | **Neon pull worker** | 백엔드가 Neon 이벤트 pull → 실시간 점수 → `prediction_log` (19-1 §9.2) |
| 7 | **scoring 어댑터** | `lightweightTreeScorer`(JS) 또는 `modelWorkerClient`(Python worker)로 실시간 점수 |
| 8 | **리텐션 push** | 고위험 → 쿠폰/추천을 Neon로 push → 시뮬 사이트 노출(루프 닫기) |

## P3 — 모델/시각화 보강
| # | 항목 | 내용 |
| --- | --- | --- |
| 9 | **세션/바운스 본격화** | 현재 경량 LogReg·Nov표본 → 풀데이터 + **GRU/TiSASRec**(시간간격) 세션 스코어러 |
| 10 | **DL→GBM 스태킹** | SASRec hidden state → LightGBM 스태킹(17-7-1 §5) |
| 11 | **진짜 SHAP** | 별도 numpy2 env에서 TreeSHAP → `shap_summary.json`(26-8) |
| 12 | **viz 3·5 보강** | Cohort Retention(코호트 summary), Train/Val Loss(부스팅 per-iter·DL epoch) |
| 13 | **business_value 시뮬** | Revenue Recovery 가정(캠페인비·성공률·잔존가치) 정식 산출 → `business_value.json` |

## P4 — 정리/운영
| # | 항목 | 내용 |
| --- | --- | --- |
| 14 | **MySQL 스냅샷 적재** | `feature_user_snapshot`/`sequence_snapshot` 채우기 + 모델파트 제출 흐름 |
| 15 | **Fastify 전환** | 내장 http → Fastify(JSON Schema 검증) |
| 16 | **얼굴 로그인 실검증** | 카메라 + buffalo_l onnx 다운로드, 등록/매칭 실제 동작 확인 |
| 17 | **S1 sample_project 정렬** | 05-6-4 구조(저우선, 백업측) |

## 권장 순서
P1(대시보드↔백엔드 연결·7모델 제출·top-risk) → P2(시뮬·Neon·실시간 루프) → P3(세션 본격화·스태킹·SHAP·시각화 보강) → P4(정리).

## 현재 동작(완료, 참고)
얼굴/아이디 로그인 · 7모델 비교(ROC/PR) · 고객 이탈조회+추천(MySQL) · 실시간 세션/바운스 · 피처중요도 · (관리자)15시각화·로그/이력(MySQL) · 백엔드 MySQL 실연동(registry·evaluation 적재).
